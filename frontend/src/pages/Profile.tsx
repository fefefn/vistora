import { User } from 'lucide-react'
import PagePlaceholder from '@/components/PagePlaceholder'

const Profile = () => (
  <PagePlaceholder
    title="Profile"
    description="Manage your account details, addresses and preferences here."
    icon={User}
  />
)

export default Profile
