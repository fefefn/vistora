import { LogIn } from 'lucide-react'
import PagePlaceholder from '@/components/PagePlaceholder'

const Login = () => (
  <PagePlaceholder
    title="Login"
    description="Sign in to your NexCart account. Authentication is added in a later phase."
    icon={LogIn}
  />
)

export default Login
