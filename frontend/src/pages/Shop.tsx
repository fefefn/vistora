import { Store } from 'lucide-react'
import PagePlaceholder from '@/components/PagePlaceholder'

const Shop = () => (
  <PagePlaceholder
    title="Shop"
    description="The product catalog with search, category filters and sorting will live here."
    icon={Store}
  />
)

export default Shop
